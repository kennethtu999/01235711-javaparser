# Spring Boot 3 Application Properties
# Server Configuration
server.port=8080
# server.servlet.context-path=/api

# Logging Configuration
logging.level.kai.javaparser=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.boot=INFO

# Jackson Configuration for Spring Boot 3
spring.jackson.default-property-inclusion=non_null
spring.jackson.serialization.write-dates-as-timestamps=false

# Actuator Configuration (if needed)
# management.endpoints.web.exposure.include=health,info,metrics

# Application Info
spring.application.name=ast-parser
spring.application.description=Java AST Parser with Spring Boot 3
